id: webview-load-url

info:
  name: Webview loadUrl usage
  author: S1rN3tZ
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - 'Landroid/webkit/WebView;->loadUrl(Ljava/lang/String;)V'
          - 'loadUrl\([^\)]*\)'
