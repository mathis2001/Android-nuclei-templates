id: deeplink-detect

info:
  name: Deeplink detection
  author: S1rN3tZ
  severity: info

file:
  - extensions:
      - xml

    extractors:
      - type: regex
        regex:
          - "android:scheme"
          - "android:host"
          - "android:pathPrefix
