id: file-scheme

info:
  name: File Scheme Enabled
  author: s1rn3tz
  severity: info

file:
  - extensions:
      - xml

    extractors:
      - type: regex
        regex:
          - "android:scheme=\"file\""
