id: firebase-detect

info:
  name: Application use Firebase DB
  author: S1rN3tZ
  severity: info

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "firebaseio.com"
          - "appspot.com"
